export const btn = (bg) => ({
  padding: "6px 10px",
  borderRadius: 8,
  border: 0,
  cursor: "pointer",
  background: bg,
  color: "#fff",
});

export const pill = () => ({
  background: "rgba(0,0,0,0.45)",
  padding: "6px 10px",
  borderRadius: 8,
});

export const input = () => ({
  width: 240,
  padding: "6px 10px",
  borderRadius: 8,
  border: 0,
  outline: "none",
  background: "rgba(255,255,255,0.15)",
  color: "#fff",
});

export const numInput = () => ({
  width: 64,
  marginLeft: 6,
  padding: "6px 8px",
  borderRadius: 8,
  border: 0,
  outline: "none",
  background: "rgba(255,255,255,0.15)",
  color: "#fff",
});